function siguienteNum(num: number) {
    let num_aux: string = " ";
    let num_final = 0;
    let cadena_num: string = num.toString();
    let num_final2: string = " ";

    for(let i = 0; i < cadena_num.length; i++) {
        
    }
    
}

const num1 = siguienteNum(456);
console.log(`${num1}`);