function isValid(cadena) {
    for (let i = 0; i < cadena.length; i++) {
        //if(cadena[i] )
    }
}
const cadena = "3hey5hello2hi";
const iV = isValid(cadena);
console.log(`Validador de mensajes = ${iV}`);
